[package]
name = "ripwing-filter"
version = "0.1.0"
edition = "2021"
description = "Pure, host-testable sensor signal conditioning (median + moving average). no_std, no HAL."

[dependencies]
ripwing-common = { path = "../common" }

[dev-dependencies]
approx = "0.5"
